# 云平台5090快速运行指南 ⚡

## 📋 5分钟快速上手

### 步骤1: 登录云平台

选择以下任一云平台（推荐AutoDL或恒源云）：

- **AutoDL** (推荐): https://www.autodl.com/
- **恒源云**: https://www.gpushare.com/
- **矩池云**: https://www.matpool.com/

选择配置：
```
GPU: RTX 5090 (24GB)
镜像: PyTorch 2.1 + Python 3.10 + CUDA 12.1
```

### 步骤2: 一键部署

登录云平台后，打开终端，运行：

```bash
# 下载并执行部署脚本
wget https://raw.githubusercontent.com/li147852xu/ARIN7101_Project_Stock_Prediction/main/deploy_cloud.sh
chmod +x deploy_cloud.sh
./deploy_cloud.sh
```

**等待约3-5分钟**，脚本会自动完成：
- ✅ 克隆项目
- ✅ 创建虚拟环境
- ✅ 安装PyTorch (CUDA 12.1)
- ✅ 安装所有依赖
- ✅ 验证GPU可用性

### 步骤3: 运行Demo

进入项目目录并激活环境：

```bash
cd ARIN7101_Project_Stock_Prediction
source venv/bin/activate
```

#### 选项A: 交互式Demo（推荐）

```bash
./demo_complete.sh
```

会出现菜单让你选择：
```
1) Quick Test (5 minutes) - 快速验证
2) Standard Demo (20 minutes) - 标准展示
3) Complete Demo (60-90 minutes) - 完整实验
```

**推荐选择2 (Standard Demo)**，适合大多数展示场景。

#### 选项B: 命令行运行

```bash
# 完整流程（自动运行所有步骤）
python main.py

# 或分步运行
python main.py --step download                    # 下载数据
python main.py --step train --model mlp,lstm     # 训练模型
python main.py --step evaluate                    # 评估
```

### 步骤4: 查看结果

```bash
# 查看模型对比
cat results/model_comparison.csv

# 列出生成的文件
ls -lh results/models/  # 训练好的模型
ls -lh plots/           # 可视化图表
```

### 步骤5: 下载结果（可选）

在本地终端运行：

```bash
# 从云平台下载结果到本地
scp -r username@cloud-server-ip:/path/to/Project/results ./
scp -r username@cloud-server-ip:/path/to/Project/plots ./
```

或使用云平台的文件下载功能。

---

## 🎯 三种Demo模式详解

### 1️⃣ Quick Test (5分钟)

**适用场景**：快速验证环境是否正常

**配置**：
- 1只股票（贵州茅台）
- 2个模型（MLP + LSTM）
- 10个训练轮次
- 2023年开始的数据

**运行**：
```bash
./demo_complete.sh
# 选择 1
```

**输出**：
- 2个训练好的模型
- 基本的评估指标
- 混淆矩阵和ROC曲线

---

### 2️⃣ Standard Demo (20分钟) ⭐推荐

**适用场景**：课程展示、项目汇报

**配置**：
- 5只股票（上证50成分股）
- 3个模型（MLP + LSTM + Transformer）
- 默认配置（50轮）
- 2019年开始的数据

**运行**：
```bash
./demo_complete.sh
# 选择 2
```

**输出**：
- 3个深度学习模型
- 完整的评估报告
- 模型对比分析
- 所有可视化图表

**预期性能**：
```
Model         Accuracy  F1-Score  AUC
─────────────────────────────────────
MLP           0.52-0.55  0.51-0.54  0.50-0.55
LSTM          0.53-0.57  0.52-0.56  0.52-0.58
Transformer   0.54-0.58  0.53-0.57  0.53-0.59
```

---

### 3️⃣ Complete Demo (60-90分钟)

**适用场景**：完整实验、论文研究

**配置**：
- 5只股票
- 6个模型（MLP + LSTM + Transformer + Mamba + ARIMA + Prophet）
- 完整训练（50轮）
- 2019年开始的数据

**运行**：
```bash
./demo_complete.sh
# 选择 3
```

**输出**：
- 所有6个模型
- 详细对比分析
- 完整的技术报告

---

## 🖥️ GPU监控

在训练的同时，可以开启GPU监控：

```bash
# 新开一个终端
./monitor_gpu.sh
```

会实时显示：
- GPU温度、功耗
- GPU和显存利用率
- 训练进程信息

或使用简单命令：

```bash
watch -n 1 nvidia-smi
```

---

## 📊 预期结果示例

### 训练日志
```
Epoch 1/50 - Train Loss: 0.6932, Train Acc: 0.5012, Val Loss: 0.6925, Val Acc: 0.5045
Epoch 2/50 - Train Loss: 0.6891, Train Acc: 0.5234, Val Loss: 0.6888, Val Acc: 0.5189
...
Epoch 50/50 - Train Loss: 0.4123, Train Acc: 0.7845, Val Loss: 0.6234, Val Acc: 0.5678
```

### 模型对比
```csv
Model,Accuracy,Precision,Recall,F1-Score,AUC
MLP,0.5423,0.5312,0.5534,0.5421,0.5456
LSTM,0.5612,0.5489,0.5734,0.5609,0.5723
Transformer,0.5734,0.5623,0.5845,0.5732,0.5867
```

### 文件结构
```
results/
├── models/
│   ├── mlp.pth (45 MB)
│   ├── lstm.pth (128 MB)
│   └── transformer.pth (95 MB)
├── model_comparison.csv
└── mlp_classification_report.txt

plots/
├── model_comparison.png
├── mlp_confusion_matrix.png
├── mlp_roc_curve.png
└── mlp_training_history.png
```

---

## ⚡ 性能优化技巧

### 1. 加速训练

```bash
# 修改配置，增大batch size
# 编辑 config.yaml:
training:
  batch_size: 512  # RTX 5090可以使用更大batch
```

### 2. 减少数据量（快速测试）

```bash
# 只使用1只股票
data:
  stock_codes:
    - "600519.SS"
```

### 3. 减少训练轮次

```bash
training:
  epochs: 20  # 从50降到20
```

---

## ❓ 常见问题

### Q1: 部署脚本运行失败

**A**: 检查网络连接，尝试手动克隆：
```bash
git clone https://github.com/li147852xu/ARIN7101_Project_Stock_Prediction.git
cd ARIN7101_Project_Stock_Prediction
pip install -r requirements.txt
```

### Q2: CUDA Out of Memory

**A**: 减小batch size：
```yaml
training:
  batch_size: 64  # 从256降到64
```

### Q3: 数据下载慢或失败

**A**: 使用国内数据源：
```yaml
data:
  data_source: "akshare"  # 从yfinance改为akshare
```

### Q4: Mamba安装失败

**A**: 不用担心，系统会自动使用GRU作为替代，不影响使用。

### Q5: 如何停止训练？

**A**: 按 `Ctrl+C`，模型会保存当前进度。

---

## 📱 远程访问结果

### 方法1: 启动Web服务器

```bash
# 在plots目录启动HTTP服务器
cd plots
python -m http.server 8000

# 在浏览器访问: http://[云服务器IP]:8000
```

### 方法2: 使用Jupyter

```bash
pip install jupyter
jupyter notebook --ip=0.0.0.0 --port=8888 --no-browser

# 访问: http://[云服务器IP]:8888
```

### 方法3: 下载到本地

在本地运行：
```bash
scp -r user@server:/path/to/Project/plots ./
scp -r user@server:/path/to/Project/results ./
```

---

## 🎓 Demo演示提示

在演示时可以：

1. **开始前**：展示GPU配置 `nvidia-smi`
2. **训练中**：展示实时GPU监控
3. **训练后**：展示模型对比结果
4. **可视化**：展示混淆矩阵、ROC曲线
5. **代码**：展示核心代码片段

---

## 📞 获取帮助

- **文档**: 查看 [CLOUD_GPU_DEPLOYMENT.md](CLOUD_GPU_DEPLOYMENT.md) 详细文档
- **项目**: https://github.com/li147852xu/ARIN7101_Project_Stock_Prediction
- **配置**: 查看和修改 `config.yaml`
- **日志**: 查看 `logs/` 目录

---

## ✅ 检查清单

运行Demo前确认：
- [ ] GPU可用 (`nvidia-smi` 显示RTX 5090)
- [ ] Python 3.8+ 已安装
- [ ] 虚拟环境已激活
- [ ] 所有依赖已安装 (`python test_setup.py`)
- [ ] 网络连接正常

运行Demo后验证：
- [ ] 模型文件已生成 (`ls results/models/`)
- [ ] 图表已生成 (`ls plots/`)
- [ ] 日志正常 (`cat logs/*.log`)

---

**开始你的Demo之旅吧！** 🚀

如有问题，请查看详细文档或检查日志文件。

